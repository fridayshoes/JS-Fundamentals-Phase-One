const people = ['john', 'phil', 'miles', 'louise'];

// print the 2nd name of the array
console.log(people[1]);

// create a new array by adding another name, then print the new array
const newPeople = people.concat('adam');
console.log(newPeople);